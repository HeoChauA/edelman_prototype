{% block feature %}
  {% for post in posts %}
    <div class="feature">
      <div class="page__title__wrap clearfix">
        <div class="page__title__image" style="background-image: url({{ post.thumbnail }})"> </div>
        <div class="page__title__container container">
          <h1 class="page__title">{{post.title}}</h1>
        </div>
      </div>
    </div>
  {% endfor %}
{% endblock %}
